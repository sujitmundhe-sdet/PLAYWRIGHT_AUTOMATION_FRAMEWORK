# Core test runtime
pytest>=7.0

# Playwright and pytest integration
playwright>=1.58.0
pytest-playwright>=0.7.0

# Config loader used in conftest.py
PyYAML>=6.0

# Optional (recommended for CI)
pytest-xdist>=3.8.0       # parallel test execution (use with -n auto)
pytest-html>=4.2.0       # generate HTML reports