# PLAYWRIGHT_AUTOMATION_FRAMEWORK

Lightweight Playwright + pytest automation framework.

## Requirements
- Python 3.10+
- Node.js (for Playwright) — Playwright installer will fetch browsers

## Quick setup
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m playwright install --with-deps